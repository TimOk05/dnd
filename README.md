# D&D Copilot - Генератор персонажей и противников

> **⚠️ ВАЖНО: Этот файл должен обновляться при любых изменениях в проекте!**
> 
> **Правила обновления README:**
> - При добавлении новых рас/классов - обновить списки в разделе "Основные возможности"
> - При изменении структуры API - обновить раздел "Техническая архитектура"
> - При добавлении новых функций - обновить соответствующие разделы
> - При изменении количества строк в файлах - обновить "Структура проекта"
> - При решении новых проблем - добавить в раздел "Известные проблемы"
> - При изменении версии - обновить "Версия" в конце файла
> 
> **Последнее обновление README**: Август 2024

Веб-приложение для генерации персонажей и противников для настольной ролевой игры Dungeons & Dragons 5e с интеграцией AI для создания описаний и тактики.

## 🚀 Основные возможности

### Генерация персонажей
- **22 расы**: Человек, Эльф, Дварф, Полурослик, Орк, Тифлинг, Драконорожденный, Гном, Полуэльф, Полуорк, Табакси, Ааракокра, Гоблин, Кенку, Ящеролюд, Тритон, Юань-ти, Голиаф, Фирболг, Багбир, Хобгоблин, Кобольд
- **13 классов**: Воин, Волшебник, Плут, Жрец, Следопыт, Варвар, Бард, Друид, Монах, Паладин, Чародей, Колдун, Изобретатель
- **AI-описания**: Автоматическое создание описаний и предысторий персонажей через DeepSeek API
- **Fallback система**: При недоступности AI используются качественные предустановленные описания
- **Полные характеристики**: Хиты, КД, скорость, инициатива, урон, владения, заклинания, снаряжение
- **Случайные параметры**: Мировоззрение, пол, профессия из JSON базы данных

### Генерация противников
- **Расширенная база данных**: 19+ монстров разных уровней сложности (CR 1/8 - 13)
- **AI-описания**: Тактика и поведение противников
- **Боевые характеристики**: Полная информация для боя
- **Фильтрация**: По уровню угрозы, типу, окружению

### Дополнительные функции
- **Сохранение в заметки**: Сохранение персонажей и противников в сессии
- **Система инициативы**: Добавление персонажей из заметок в инициативу
- **Мобильная версия**: Адаптивный дизайн для всех устройств
- **PWA поддержка**: Можно установить как приложение

## 📁 Структура проекта

```
dnd/
├── index.php                    # Главная страница приложения (2469 строк)
├── template.html                # HTML шаблон и CSS стили (3085 строк)
├── mobile.js                    # Мобильные функции и PWA (760 строк)
├── config.php                   # Конфигурация API ключей
├── api/                         # API файлы
│   ├── generate-characters.php  # Генерация персонажей (885 строк)
│   ├── generate-enemies.php     # Генерация противников (492 строки)
│   ├── dnd-api-working.php      # Работа с D&D 5e API (662 строки)
│   ├── deepseek.php             # AI описания через DeepSeek (161 строка)
│   ├── fallback-data.php        # Локальные данные (602 строки)
│   └── error-handler.php        # Обработка ошибок (160 строк)
├── pdf/                         # JSON файлы с данными
│   ├── d100_unique_traders.json # Профессии и торговцы
│   └── dnd_npc_mechanics_context_v2.json # Механики D&D
├── РЕШЕНИЕ_ПРОБЛЕМЫ.md          # Документация исправлений
└── README.md                    # Этот файл
```

## 🔧 Техническая архитектура

### Frontend (index.php)
- **Основной интерфейс**: Модальные окна для генерации персонажей и противников
- **JavaScript функции**:
  - `formatCharacterFromApi()` - форматирование персонажей
  - `formatEnemiesFromApi()` - форматирование противников
  - `saveCharacterToNotes()` - сохранение персонажей
  - `saveEnemyToNotes()` - сохранение противников
  - `addFromNotes()` - добавление из заметок в инициативу
- **AJAX запросы**: Для взаимодействия с API
- **Адаптивный дизайн**: CSS Grid и Flexbox

### Backend API

#### generate-characters.php
```php
class CharacterGenerator {
    // Основные методы:
    - generateCharacter($params)     // Главная функция генерации
    - generateAbilities($race_data)  // Генерация характеристик
    - generateName($race, $gender)   // Генерация имени
    - getRandomOccupation()          // Случайная профессия
    - generateDescription($character) // AI описание
    - generateBackground($character)  // AI предыстория
}
```

#### generate-enemies.php
```php
class EnemyGenerator {
    // Основные методы:
    - generateEnemies($params)       // Главная функция генерации
    - filterMonsters($monsters, $params) // Фильтрация монстров
    - generateEnemyDescription($enemy)   // AI описание
}
```

#### dnd-api-working.php
```php
class DndApiWorking {
    // Основные методы:
    - makeRequest($endpoint, $method = 'GET') // HTTP запросы
    - getRaceInfo($race_index)               // Информация о расе
    - getClassInfo($class_index)             // Информация о классе
    - getMonsters()                          // Список монстров
}
```

### Интеграции

#### D&D 5e API (https://www.dnd5eapi.co/api)
- **Расы**: `/api/races/{index}`
- **Классы**: `/api/classes/{index}`
- **Монстры**: `/api/monsters`
- **Fallback**: Локальные данные в `fallback-data.php`

#### DeepSeek AI API
- **Описания персонажей**: Детальные описания с учетом всех характеристик
- **Предыстории**: Создание биографий персонажей
- **Тактика противников**: Описание поведения в бою

## 🎯 Ключевые функции

### Генерация персонажей
1. **Валидация параметров**: Уровень 1-20, валидные расы/классы
2. **Генерация характеристик**: 4d6 drop lowest + бонусы расы
3. **Расчет параметров**: Хиты, КД, скорость, инициатива, урон
4. **AI описание**: Промпт с полной информацией о персонаже
5. **Сохранение**: В сессии для использования в инициативе

### Генерация противников
1. **Фильтрация**: По CR, типу, окружению
2. **Случайный выбор**: Из подходящих монстров
3. **AI описание**: Тактика и поведение
4. **Массовое сохранение**: Всех противников сразу

### Система заметок
- **Сохранение**: HTML формат с полной информацией
- **Парсинг**: Извлечение имени и инициативы
- **Интеграция**: С системой инициативы

## 🔧 Установка и настройка

### Требования
- PHP 7.4+ с расширением cURL
- Веб-сервер (Apache/Nginx)
- API ключ DeepSeek

### Настройка config.php
```php
function getApiKey($service) {
    $keys = [
        'deepseek' => 'your-deepseek-api-key-here'
    ];
    return $keys[$service] ?? null;
}
```

### Структура данных

#### Персонаж
```php
$character = [
    'name' => 'Имя',
    'race' => 'Раса',
    'class' => 'Класс',
    'level' => 5,
    'alignment' => 'Мировоззрение',
    'gender' => 'Пол',
    'occupation' => 'Профессия',
    'abilities' => ['str' => 15, 'dex' => 14, ...],
    'hit_points' => 27,
    'armor_class' => 15,
    'speed' => 30,
    'initiative' => 2,
    'proficiency_bonus' => 3,
    'damage' => '8 + 3',
    'proficiencies' => [...],
    'spells' => [...],
    'equipment' => [...],
    'saving_throws' => [...],
    'description' => 'AI описание',
    'background' => 'AI предыстория'
];
```

#### Противник
```php
$enemy = [
    'name' => 'Название',
    'type' => 'гуманоид',
    'size' => 'средний',
    'alignment' => 'законно-нейтральный',
    'challenge_rating' => '1/4',
    'hit_points' => 15,
    'armor_class' => 13,
    'speed' => '30 футов',
    'abilities' => ['str' => 16, 'dex' => 12, ...],
    'actions' => ['атака' => 'описание'],
    'special_abilities' => ['способность' => 'описание'],
    'description' => 'AI описание'
];
```

## 🐛 Известные проблемы и решения

### Проблемы с cURL
- **Симптом**: "cURL недоступен" - AI описания не генерируются
- **Решение**: Установить расширение cURL для PHP или использовать fallback описания
- **Текущее состояние**: При отсутствии cURL используются качественные fallback описания

### Проблемы с API
- **Симптом**: Ошибки HTTP запросов
- **Решение**: Fallback на локальные данные в `fallback-data.php`

### Проблемы с валидацией
- **Симптом**: "Ошибка генерации характеристик"
- **Решение**: Диапазон характеристик 3-20, проверка в `generate-characters.php`

## 🔄 Процесс разработки

### Добавление новой расы
1. Добавить в `$valid_races` в `generate-characters.php`
2. Добавить данные в `getRaceData()` в `generate-characters.php`
3. Добавить имена в `getNamesByRace()` в `generate-characters.php`
4. Добавить в интерфейс в `index.php`

### Добавление нового класса
1. Добавить в `$valid_classes` в `generate-characters.php`
2. Добавить данные в `getClassData()` в `generate-characters.php`
3. Добавить в интерфейс в `index.php`

### Добавление нового монстра
1. Добавить в `getMonsters()` в `fallback-data.php`
2. Убедиться в корректности CR и типа

## 📱 Мобильная версия

### Адаптивный дизайн
- **CSS Grid**: Для десктопной версии
- **Flexbox**: Для мобильной версии
- **Медиа-запросы**: `@media (max-width: 768px)`

### PWA функции
- **Service Worker**: `sw.js`
- **Manifest**: `manifest.json`
- **Офлайн режим**: `offline.html`

## 🎨 Стилизация

### CSS переменные
```css
:root {
    --bg-primary: #1a1a1a;
    --bg-secondary: #2a2a2a;
    --text-primary: #ffffff;
    --accent-primary: #4a90e2;
    --accent-secondary: #f39c12;
}
```

### Компоненты
- **Модальные окна**: `.modal`, `.modal-content`
- **Карточки**: `.character-block`, `.enemy-block`
- **Кнопки**: `.generate-btn`, `.save-character-btn`
- **Сетки**: `.info-grid`, `.abilities-grid`

## 🔍 Отладка

### Логирование
- `error_log()` в критических местах
- Проверка `$_SESSION['notes']` для заметок
- Валидация входных параметров

### Тестирование
- Проверка всех рас и классов
- Тестирование AI описаний
- Проверка мобильной версии

## 📚 Дополнительные ресурсы

- **D&D 5e API**: https://www.dnd5eapi.co/api
- **DeepSeek API**: https://platform.deepseek.com/
- **PWA документация**: https://web.dev/progressive-web-apps/

## 🚀 Планы развития

- [ ] Добавление новых рас и классов
- [ ] Расширение базы монстров
- [ ] Улучшение AI промптов
- [ ] Добавление системы заклинаний
- [ ] Интеграция с другими D&D API

---

**Версия**: 2.0  
**Последнее обновление**: Август 2024  
**Автор**: D&D Copilot Team

---

> **📝 Напоминание для разработчиков:**
> 
> **Этот README файл является живым документом и должен поддерживаться в актуальном состоянии!**
> 
> **При каждом изменении проекта:**
> 1. Обновите соответствующие разделы README
> 2. Измените дату "Последнее обновление"
> 3. При необходимости обновите версию
> 4. Убедитесь, что документация соответствует текущему состоянию кода
> 
> **Помните:** Хорошая документация экономит время при дальнейшей разработке и помогает новым разработчикам быстро понять проект.
