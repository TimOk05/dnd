# DnD Copilot - Помощник мастера D&D

Полнофункциональное веб-приложение для мастера D&D с системой регистрации пользователей, генерацией NPC, управлением инициативой и AI-чатом.

## 🚀 Возможности

### 🎲 Основные функции
- **Генерация NPC** - создание уникальных персонажей с выбором расы, класса и уровня
- **Система инициативы** - управление боевыми ходами с красивым интерфейсом
- **Бросок костей** - поддержка всех стандартных костей D&D
- **AI Чат** - интеграция с DeepSeek API для генерации контента
- **Заметки** - сохранение и управление игровыми заметками

### 👤 Система пользователей
- **Регистрация и авторизация** - безопасная система с валидацией
- **Профиль пользователя** - управление аккаунтом и смена пароля
- **Сессии** - безопасное управление сессиями с токенами
- **Роли** - система ролей (пользователь/администратор)

### 🎨 Дизайн
- **Две темы** - светлая (пергаментная) и тёмная (фиолетовая)
- **Адаптивный дизайн** - полная поддержка мобильных устройств
- **Атмосферный стиль** - дизайн в стиле D&D с текстурами и анимациями

## 📋 Требования

- PHP 7.4 или выше
- MySQL 5.7 или выше
- Веб-сервер (Apache/Nginx)
- Расширение PHP PDO
- Расширение PHP JSON

## 🔧 Установка

### 1. Настройка базы данных

Откройте файл `config.php` и настройте подключение к базе данных:

```php
define('DB_HOST', 'localhost');
define('DB_NAME', 'dnd_copilot');
define('DB_USER', 'your_username');
define('DB_PASS', 'your_password');
```

### 2. Автоматическая настройка

Приложение автоматически настроит базу данных при первом запуске. Просто откройте:

```
http://your-domain.com/index.php
```

Система автоматически:
- Создаст базу данных и все необходимые таблицы
- Создаст администратора с данными:
  - **Логин**: `admin`
  - **Email**: `admin@dndcopilot.local`
  - **Пароль**: `TimOkdndAdm1n`
- Автоматически войдет в систему как администратор

Этот скрипт:
- Создаст базу данных
- Создаст все необходимые таблицы
- Создаст администратора по умолчанию

### 3. Настройка API ключа

В файле `config.php` укажите ваш API ключ DeepSeek:

```php
define('DEEPSEEK_API_KEY', 'your-deepseek-api-key');
```

### 4. Первый вход

После установки используйте данные администратора по умолчанию:
- **Имя пользователя**: `admin`
- **Email**: `admin@dndcopilot.local`
- **Пароль**: `TimOkdndAdm1n`

**Важно**: Смените пароль администратора после первого входа!

## 🗄️ Структура базы данных

### Таблицы

- **users** - пользователи системы
- **user_sessions** - активные сессии
- **user_notes** - заметки пользователей
- **user_chat** - история чата
- **login_attempts** - попытки входа (для безопасности)

### Безопасность

- Хеширование паролей с солью (bcrypt)
- Защита от брутфорс атак
- Валидация всех входных данных
- Безопасные сессии с токенами
- Защита от SQL-инъекций

## 📱 Использование

### Регистрация нового пользователя

1. Перейдите на страницу входа (`login.php`)
2. Нажмите "Зарегистрироваться"
3. Заполните форму с валидными данными
4. Войдите в систему

### Генерация NPC

1. Нажмите кнопку "🗣️ NPC"
2. Выберите расу персонажа
3. Выберите класс
4. Укажите уровень (1-20)
5. Нажмите "Создать NPC"
6. Сохраните в заметки при необходимости

### Управление инициативой

1. Нажмите кнопку "⚡ Инициатива"
2. Добавьте участников (игроки, противники, другие)
3. Укажите значения инициативы
4. Используйте кнопки навигации для управления ходами
5. Сохраните в заметки

### Бросок костей

1. Нажмите кнопку "🎲 Бросок костей"
2. Выберите тип кости (d3, d4, d6, d8, d10, d12, d20, d100)
3. Укажите количество бросков
4. Добавьте комментарий (необязательно)
5. Получите результат

## 🔒 Безопасность

### Требования к паролю
- Минимум 8 символов
- Хотя бы одна заглавная буква
- Хотя бы одна строчная буква
- Хотя бы одна цифра

### Защита от атак
- Ограничение попыток входа (5 попыток, блокировка на 15 минут)
- Логирование всех попыток входа
- Автоматическое удаление истекших сессий
- Валидация всех пользовательских данных

## 🎨 Настройка темы

Приложение поддерживает две темы:
- **Светлая** - классическая пергаментная тема
- **Тёмная** - современная фиолетовая тема

Тема сохраняется в localStorage браузера.

## 📁 Структура файлов

```
dnd/
├── index.php          # Главная страница приложения
├── login.php          # Страница входа/регистрации
├── profile.php        # Профиль пользователя
├── auth.php           # Обработка авторизации
├── config.php         # Конфигурация
├── setup.php          # Настройка базы данных
├── ai.php             # Обработка AI запросов
├── template.html      # HTML шаблон
├── api/               # API файлы
├── pdf/               # База данных торговцев
└── vendor/            # Зависимости
```

## 🛠️ Разработка

### Добавление новых функций

1. Создайте новый файл или отредактируйте существующий
2. Добавьте необходимые таблицы в базу данных
3. Обновите систему авторизации при необходимости
4. Протестируйте на разных устройствах

### Стилизация

Все стили находятся в `template.html`. Используйте CSS переменные для согласованности цветов:

```css
:root {
    --bg-primary: #f8ecd0;
    --text-primary: #2d1b00;
    --accent-primary: #a67c52;
    /* и т.д. */
}
```

## 📞 Поддержка

### Исправление ошибки HTTP 500

Если вы видите ошибку "HTTP ERROR 500":

1. **Проверьте настройки базы данных** в `config.php`
2. **Откройте главную страницу**: `http://your-domain.com/index.php` (автоматическая настройка)
3. **Проверьте логи ошибок** вашего веб-сервера
4. **Откройте страницу диагностики**: `http://your-domain.com/status.php`

### Общие проблемы

1. **База данных не настроена** → Откройте `index.php` (автоматическая настройка)
2. **Неправильные настройки MySQL** → Проверьте `config.php`
3. **Отсутствуют расширения PHP** → Установите PDO, JSON, cURL
4. **Проблемы с правами доступа** → Установите права 755 для папок, 644 для файлов

### Диагностика

- **Страница статуса**: `status.php` - показывает состояние всех компонентов
- **Режим отладки**: Включен в `config.php` для диагностики
- **Логи ошибок**: Проверьте логи Apache/Nginx для детальной информации

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

---

**DnD Copilot** - ваш верный помощник в мире D&D! 🎲⚔️🐉
