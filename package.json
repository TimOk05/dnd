{
    "name": "dm-copilot",
    "version": "0.1.0",
    "description": "AI-powered DnD session assistant for dungeon masters",
    "private": true,
    "scripts": {
        "dev": "npm --filter dm-copilot-web dev",
        "build": "npm --filter dm-copilot-web build",
        "start": "npm --filter dm-copilot-web start",
        "lint": "npm --filter dm-copilot-web lint",
        "db:generate": "npm --filter dm-copilot-db generate",
        "db:push": "npm --filter dm-copilot-db push",
        "db:migrate": "npm --filter dm-copilot-db migrate",
        "db:seed": "npm --filter dm-copilot-db seed"
    },
    "devDependencies": {
        "@types/node": "^20.0.0",
        "typescript": "^5.0.0"
    },
    "packageManager": "npm@8.0.0",
    "engines": {
        "node": ">=18.0.0"
    }
}